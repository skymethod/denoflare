// grab the native ones up front, they might be overridden by untrusted code later on
export const consoleLog = console.log;
export const consoleWarn = console.warn;
export const consoleError = console.error;
